import React from 'react';
import RaisedButton from 'material-ui/RaisedButton';

const QuestionButton = () => (
  <div>
    <RaisedButton label="Generate Question"  fullWidth={true} secondary={true} />
  </div>
);

export default QuestionButton;
